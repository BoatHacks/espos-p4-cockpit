# Partition table for Waveshare ESP32-P4-WIFI6-POE-ETH (16 MB flash).
# Two 6.5 MB OTA app slots, a dedicated NVS partition for the layout
# store, a SPIFFS partition SensESP's config system still mounts, 256 KB
# coredump.
# The layout store moved from the old "spiffs" LittleFS partition to the
# dedicated "layout" NVS partition: LittleFS on this flash did not
# survive a power-cycle (the superblock came back unmountable and got
# silently reformatted, dropping the persisted layout). NVS is
# wear-leveled and power-fail-safe — the same store WiFi creds use.
# The "spiffs" partition is kept (smaller) because SensESP mounts it
# fatally at boot for its Saveable config system.
# Name,    Type, SubType, Offset,   Size,    Flags
nvs,       data, nvs,     0x9000,   0x5000,
otadata,   data, ota,     0xe000,   0x2000,
app0,      app,  ota_0,   0x10000,  0x680000,
app1,      app,  ota_1,   0x690000, 0x680000,
layout,    data, nvs,     0xD10000, 0x20000,
spiffs,    data, spiffs,  0xD30000, 0x1E0000,
coredump,  data, coredump,0xF10000, 0x40000,
